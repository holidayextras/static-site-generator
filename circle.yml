machine:
  node:
    version: 4.4.4
dependencies:
  cache_directories:
    - node_modules
test:
  override:
    - npm run ci
deployment:
  production:
    branch: 2.0.4
    commands:
    - echo -e "$NPM_USERNAME\n$NPM_PASSWORD\n$NPM_EMAIL" | npm login
    - sh ./scripts/publish.sh
